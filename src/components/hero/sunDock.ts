// Shared between IntroProvider (the traveling splash sun) and Hero (the
// permanent docked mark) so the crossfade handoff lands pixel-for-pixel.
export const SUN_DOCK_TOP = 28;
export const SUN_DOCK_SIZE = 56;
